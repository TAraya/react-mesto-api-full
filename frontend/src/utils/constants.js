export const cardTemplateSelector = '#card';
export const cardListSelector = '.gallery__items';
export const submitButtonSelector = '.form__submit';

export const profileOptions = {
  nameSelector: '.profile__name',
  aboutSelector: '.profile__job',
  avatarSelector: '.profile__avatar'
};

export const profileInputOptions = {
  editButtonSelector: '.profile__edit-button',
  popupSelector: '.profile-popup',
  nameInputSelector: '.profile-name-input',
  jobInputSelector: '.profile-job-input'
};

export const cardInputOptions = {
  addButtonSelector: '.profile__add-card-button',
  popupSelector: '.add-card-popup',
  nameInputSelector: '.card-name-input',
  linkInputSelector: '.card-link-input'
};

export const avatarInputOptions = {
  popupSelector: '.avatar-popup',
  inputSelector: '.avatar-input'
};

export const cardRemovePopupSelector = '.card-remove-popup';

export const photoPopupSelector = '.photo-popup';

export const validationOptions = {
  inputSelector: '.form__input',
  submitSelector: '.form__submit',
  errorElementPostfix: '-error',
  inputErrorClass: 'form__input_error',
  errorElementActiveClass: 'form__input-error_active',
  submitInactiveClass: 'form__submit_inactive'
};